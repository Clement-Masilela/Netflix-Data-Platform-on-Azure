# Configuration Files

## Purpose
Centralized configuration for pipeline parameters, connection strings, and environment settings.

## Files
- `pipeline_config.json` - Pipeline-specific parameters
- `environment_config.json` - Environment-specific settings (dev/prod)

## Usage
Configuration files enable:
- Environment portability
- Easy parameter updates without code changes
- Separation of code and configuration
